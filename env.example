OPENAI_API_KEY=
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
GITHUB_TOKEN=
GITHUB_REPO=dlatevilawson/talkforge
# Optional privileged key for admin migrations only (never expose to the browser)
SUPABASE_SERVICE_ROLE_KEY=

# Atlas target runtime plane (ATLAS-P3). Default off — Legacy Ask Atlas serves Founder.
# ATLAS_RUNTIME_TARGET=on enables shadow target pipeline alongside Legacy.
# ATLAS_RUNTIME_FOUNDER_VISIBLE=on (requires TARGET) serves Founder from target plane.
# ATLAS_RUNTIME_TARGET=
# ATLAS_RUNTIME_FOUNDER_VISIBLE=
